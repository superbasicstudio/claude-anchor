# Security hardening for npm
# Require exact versions when adding new dependencies
save-exact=true

# Ensure package-lock.json is always generated and respected
package-lock=true

# Audit packages on install
audit=true

# Refuse to install if audit finds high/critical vulnerabilities
audit-level=high

# Prevent lifecycle scripts from running during install (defense against postinstall attacks)
ignore-scripts=false
